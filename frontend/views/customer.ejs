<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Customerlist</title>
</head>
<body>
    
    <table>
        <tr>
            <th>ID</th>
            <th>Customer NAME</th>
            <th>Order</th>
            <th>Country</th>
        </tr>
        <% for(let customer of customers) { %>
            <tr>
           <td name = "id"><a href="editCustomer/<%= customer.id %>"> <%= customer.id %></a></td>
           <td name = "cName"><%= customer.customer %></td>
           <td name = "cOrder"><%= customer.order %></td>
           <td name = "cCountry"> <%= customer.country %></td>
           <td> 
            <form action="http://127.0.0.1:3000/deleteCustomer/<%= customer.id %>" method="post">
                <button type="submit" class="btn-link">&times</button>
            </form>
           </td>
               
            <!--<a href="deleteCustomer/:<%= customer.id %>">&times</a></td>-->
            </tr>
       <% } %>
    </table>

</body>
</html>